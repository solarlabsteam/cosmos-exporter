name: GitHub Actions
on: [push]
jobs:
  go-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-go@v2
      - run: go version
      - run: go mod download
      - run: go build main.go
